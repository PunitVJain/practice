#  lambda function in python.
#  properties of lambda function
#1. lambda function is anonimus function(does not have name).
#2. lambda function will take any number of arguments.
#3. lambda function will have single expression(statement) to it.
#4. Lambda functions can be used to return function objects.
#5. 
add = lambda num_one, num_two: num_one + num_two
print(add(2, 4))